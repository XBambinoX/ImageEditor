<Window x:Class="ImageEditor.Views.BrightnessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ImageEditor.ViewModels"
        Title="Brightness"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#2D2D30"
        Icon="/Assets/MonoFrame.ico">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- PREVIEW -->
        <Border Grid.Row="0"
                Background="#1E1E1E"
                CornerRadius="4"
                Padding="5">

            <Image Source="{Binding PreviewImage}"
                   Stretch="Uniform"/>
        </Border>

        <!-- CONTROLS -->
        <Grid Grid.Row="1" Margin="0,15,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0"
                Orientation="Horizontal"
                VerticalAlignment="Center"
                Margin="0,0,0,8">
                
                <TextBlock Text="Brightness:"
                   Foreground="White"
                   VerticalAlignment="Center"
                   Width="70"
                   Margin="0,0,10,0"/>
                
                <Button Content="-"
                Width="30" Height="25"
                Margin="0,0,5,0"
                Command="{Binding DecreaseBrightnessCommand}"/>
                
                <Slider Width="300"
                Minimum="{Binding MinBrightness}"
                Maximum="{Binding MaxBrightness}"
                Value="{Binding Brightness, Mode=TwoWay}"
                TickFrequency="1"
                IsSnapToTickEnabled="True"
                PreviewMouseWheel="Brightness_MouseWheel"/>
                
                <Button Content="+"
                Width="30" Height="25"
                Margin="5,0,10,0"
                Command="{Binding IncreaseBrightnessCommand}"/>
                
                <TextBox Width="60" Height="25"
                 Text="{Binding Brightness, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalContentAlignment="Center"/>
                
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="0"
                Orientation="Horizontal"
                VerticalAlignment="Center">
                
                <TextBlock Text="Contrast:"
                   Foreground="White"
                   VerticalAlignment="Center"
                   Width="70"
                   Margin="0,0,10,0"/>
                
                <Button Content="-"
                Width="30" Height="25"
                Margin="0,0,5,0"
                Command="{Binding DecreaseContrastCommand}"/>
                
                <Slider Width="300"
                Minimum="{Binding MinContrast}"
                Maximum="{Binding MaxContrast}"
                Value="{Binding Contrast, Mode=TwoWay}"
                TickFrequency="1"
                IsSnapToTickEnabled="True"
                PreviewMouseWheel="Contrast_MouseWheel"/>
                
                <Button Content="+"
                Width="30" Height="25"
                Margin="5,0,10,0"
                Command="{Binding IncreaseContrastCommand}"/>
                
                <TextBox Width="60" Height="25"
                 Text="{Binding Contrast, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalContentAlignment="Center"/>
                
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                Orientation="Horizontal"
                VerticalAlignment="Center">
                <Button Content="OK"
                Width="80"
                Margin="0,0,10,0"
                Command="{Binding ApplyCommand}"/>
                
                <Button Content="Cancel"
                Width="80"
                Command="{Binding CancelCommand}"/>
            </StackPanel>
            
        </Grid>
    </Grid>
</Window>