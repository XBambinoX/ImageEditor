<Window x:Class="ImageEditor.Views.BlurWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:vm="clr-namespace:ImageEditor.ViewModels" 
        WindowStyle="None" AllowsTransparency="False" 
        Title="Gaussian Blur" 
        Height="600" 
        Width="900" 
        WindowStartupLocation="CenterOwner" 
        ResizeMode="CanResize" 
        Background="#2D2D30" 
        Icon="/Assets/MonoFrame.ico">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- PREVIEW AREA -->
        <Border Grid.Row="0"
                Background="#1E1E1E"
                CornerRadius="4"
                Padding="5">

            <Image Source="{Binding PreviewImage}"
                   Stretch="Uniform"/>
        </Border>

        <!-- CONTROLS -->
        <Grid Grid.Row="1" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center">

                <TextBlock Text="Radius:"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>

                <Button Content="-"
                        Width="30"
                        Height="25"
                        Margin="0,0,5,0"
                        Command="{Binding DecreaseRadiusCommand}"/>

                <Slider Width="300"
                    Minimum="1"
                    Maximum="100"
                    TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    Value="{Binding Radius, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    PreviewMouseWheel="Radius_MouseWheel"/>

                <Button Content="+"
                        Width="30"
                        Height="25"
                        Margin="5,0,10,0"
                        Command="{Binding IncreaseRadiusCommand}"/>

                <TextBox Width="60"
                         Height="25"
                         Text="{Binding Radius, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         HorizontalContentAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal">

                <Button Content="OK"
                        Width="80"
                        Margin="0,0,10,0"
                        Command="{Binding ApplyCommand}"/>

                <Button Content="Cancel"
                        Width="80"
                        Command="{Binding CancelCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>