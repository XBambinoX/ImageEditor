<Window x:Class="ImageEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ImageEditor.ViewModels"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="#303030"
        WindowState="Maximized"
        Title="MonoFrame">

    <!-- ViewModel -->
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Main colors -->
        <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="#303030"/>
        <SolidColorBrush x:Key="BorderBackgroundBrush" Color="#444444"/>
        <SolidColorBrush x:Key="TextForegroundBrush" Color="White"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BorderBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="{StaticResource PrimaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
        </Style>

        <Style TargetType="Menu">
            <Setter Property="Background" Value="{StaticResource PrimaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="{StaticResource PrimaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
        </Style>
    </Window.Resources>

    <DockPanel>

        <!-- TITLEBAR -->
        <Border DockPanel.Dock="Top"
                Background="{StaticResource PrimaryBackgroundBrush}"
                Height="30"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                BorderBrush="#555555"
                BorderThickness="0,0,0,1">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title -->
                <TextBlock Text="MonoFrame"
                           VerticalAlignment="Center"
                           Margin="10,0"/>

                <!-- Control Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="—" Width="30" Height="25" Command="{Binding MinimizeCommand}"/>
                    <Button Content="▢" Width="30" Height="25" Command="{Binding MaximizeRestoreCommand}"/>
                    <Button Content="X" Width="30" Height="25" Command="{Binding CloseCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- MENU -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Command="{Binding OpenImageCommand}"/>
                <MenuItem Header="_Save" Command="{Binding SaveImageCommand}"/>
                <Separator/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}"/>
            </MenuItem>

            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo" Command="{Binding UndoCommand}"/>
                <MenuItem Header="_Redo" Command="{Binding RedoCommand}"/>
            </MenuItem>

            <MenuItem Header="_Help">
                <MenuItem Header="_About" Command="{Binding AboutCommand}"/>
            </MenuItem>
        </Menu>

        <!-- MAIN CONTENT -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- IMAGE AREA -->
            <Border Grid.Row="0" Margin="10" CornerRadius="5" Background="Transparent">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Image Source="{Binding Image}" Stretch="None" RenderTransformOrigin="0.5,0.5"
                           MouseLeftButtonDown="Image_MouseLeftButtonDown"
                           MouseMove="Image_MouseMove"
                           MouseLeftButtonUp="Image_MouseLeftButtonUp">
                        
                        <Image.RenderTransform>
                            <TranslateTransform X="{Binding ImageOffsetX}" Y="{Binding ImageOffsetY}"/>
                        </Image.RenderTransform>
                        
                    </Image>
                </ScrollViewer>
            </Border>

            <!-- STATUSBAR -->
            <StatusBar Grid.Row="1" Padding="5">
                <StatusBarItem>
                    <TextBlock Text="{Binding StatusText}"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>

    </DockPanel>
</Window>
